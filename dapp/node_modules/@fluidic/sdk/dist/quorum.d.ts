import type { QuorumResponse } from "./types.js";
/** Tracks finalized quorum ticks and helps clients pick consistent read ticks. */
export declare class QuorumTracker {
    private latestQuorumTick;
    update(tick: number, finalized: boolean): void;
    get latest(): number;
    /** Build a query string snippet for the current latest quorum tick. */
    query(): string;
}
export declare function isFinalized(response: QuorumResponse): boolean;
export declare function parseQuorumResponse(response: QuorumResponse): {
    finalized: boolean;
    tick: number;
    stake?: bigint;
    threshold?: bigint;
    roots?: QuorumResponse["roots"];
};
